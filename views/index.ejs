<%
  const content = `
  <section class="fade-up grid gap-6 lg:grid-cols-5">
    <!-- LEFT -->
    <div class="lg:col-span-3">
      <div class="rounded-2xl bg-white shadow-soft ring-1 ring-slate-200">
        <div class="p-5 sm:p-7">
          <div class="flex items-start justify-between gap-4">
            <div>
              <h1 class="text-xl sm:text-2xl font-semibold tracking-tight">Checkout QRIS</h1>
              <p class="mt-1 text-sm text-slate-600">
                Masukkan detail transaksi, lalu generate QR untuk pembayaran.
              </p>
            </div>
            <div class="hidden sm:flex h-11 w-11 items-center justify-center rounded-xl bg-slate-900 text-white">
              <i class="ri-sparkling-2-line"></i>
            </div>
          </div>

          <form action="/pay" method="POST" class="mt-6 space-y-4">
            <div class="grid gap-4 sm:grid-cols-2">
              <label class="block">
                <span class="text-sm font-medium text-slate-700">Nama item</span>
                <input name="itemName" required
                  class="mt-2 w-full rounded-xl border border-slate-200 bg-white px-4 py-3 text-sm
                         outline-none focus:border-slate-900 focus:ring-4 focus:ring-slate-900/10"
                  placeholder="Contoh: Kopi Susu" />
              </label>

              <label class="block">
                <span class="text-sm font-medium text-slate-700">Qty</span>
                <input name="qty" type="number" min="1" value="1" required
                  class="mt-2 w-full rounded-xl border border-slate-200 bg-white px-4 py-3 text-sm
                         outline-none focus:border-slate-900 focus:ring-4 focus:ring-slate-900/10" />
              </label>
            </div>

            <label class="block">
              <span class="text-sm font-medium text-slate-700">Nominal (IDR)</span>
              <div class="mt-2 flex items-center gap-2 rounded-xl border border-slate-200 bg-white px-4 py-3
                          focus-within:border-slate-900 focus-within:ring-4 focus-within:ring-slate-900/10">
                <i class="ri-money-dollar-circle-line text-slate-500"></i>
                <input name="amount" type="number" min="1000" step="1000" required
                  class="w-full bg-transparent text-sm outline-none"
                  placeholder="Misal: 25000" />
              </div>
              <p class="mt-2 text-xs text-slate-500">
                Minimal demo 1000. Production ikuti aturan channel pembayaran.
              </p>
            </label>

            <div class="flex flex-col sm:flex-row gap-3 sm:items-center sm:justify-between pt-2">
              <button type="submit"
                class="inline-flex w-full sm:w-auto items-center justify-center gap-2 rounded-xl bg-slate-900 px-5 py-3
                       text-sm font-semibold text-white shadow-sm hover:opacity-95 active:opacity-90 transition">
                <i class="ri-qr-code-line"></i>
                Buat QRIS
              </button>

              <div class="text-xs text-slate-500 inline-flex items-center gap-2">
                <i class="ri-flashlight-line"></i>
                Proses cepat, tampilan bersih, mobile-first.
              </div>
            </div>
          </form>
        </div>
      </div>
    </div>

    <!-- RIGHT -->
    <aside class="lg:col-span-2">
      <div class="rounded-2xl bg-white shadow-soft ring-1 ring-slate-200">
        <div class="p-5 sm:p-7">
          <h2 class="text-base font-semibold">Alur Singkat</h2>
          <ol class="mt-4 space-y-3 text-sm text-slate-600">
            <li class="flex gap-3">
              <span class="mt-0.5 h-6 w-6 shrink-0 rounded-lg bg-slate-900 text-white flex items-center justify-center text-xs">1</span>
              Backend hit <code class="px-2 py-0.5 rounded bg-slate-100 text-slate-700">/charge</code> dengan <code class="px-2 py-0.5 rounded bg-slate-100 text-slate-700">payment_type: qris</code>.
            </li>
            <li class="flex gap-3">
              <span class="mt-0.5 h-6 w-6 shrink-0 rounded-lg bg-slate-900 text-white flex items-center justify-center text-xs">2</span>
              Tampilkan QR dari response (umumnya <code class="px-2 py-0.5 rounded bg-slate-100 text-slate-700">actions[].url</code>).
            </li>
            <li class="flex gap-3">
              <span class="mt-0.5 h-6 w-6 shrink-0 rounded-lg bg-slate-900 text-white flex items-center justify-center text-xs">3</span>
              Cek status via polling atau webhook.
            </li>
            <li class="flex gap-3">
              <span class="mt-0.5 h-6 w-6 shrink-0 rounded-lg bg-slate-900 text-white flex items-center justify-center text-xs">4</span>
              Webhook diverifikasi signature_key sebelum update status.
            </li>
          </ol>

          <div class="mt-6 rounded-xl border border-slate-200 bg-slate-50 p-4 text-sm text-slate-700">
            <div class="flex items-center gap-2 font-semibold">
              <i class="ri-information-line"></i> Catatan
            </div>
            <p class="mt-2 text-sm text-slate-600">
              Demo ini fokus UX + flow. Production: simpan ke DB, idempotency, dan HTTPS.
            </p>
          </div>
        </div>
      </div>
    </aside>
  </section>
  `;
%>

<%- include("layout", { title, body: content }) %>
